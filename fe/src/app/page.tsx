import styles from "./page.module.css";

const Home = () => {
    return (
        <main className={styles.main}>
            {/* TODO Create a diagram/table/or something else for the important things */}
            {/* TODO Remove the old stuff */}
            {/* TODO Create a new main page for Zoo */}
            <h1>🦁 Zoo 🦁</h1>
        </main>
    );
};

export default Home;
